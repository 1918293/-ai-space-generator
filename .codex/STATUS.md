Status: IMPLEMENTATION_CANDIDATE_READY on `auto/chenghe428-v0.3` at application commit `2cf2b7db484890afd0084838e855d84c78fdf2c2`. The current-main-compatible v0.3 recovery candidate modifies only `app.js`, `index.html`, and `styles.css`. Local syntax/static checks PASS and the separately created recovery invariant self-test is 19/19 PASS. This recovery test is not the original handoff `runtime_selftest.js`; that original artifact remains unavailable, so final promotion is `ORIGINAL_SELFTEST_MISSING`. Keep `.codex/DO_NOT_MERGE.md`; do not merge or open the final PR until the original self-test boundary is resolved or Hao explicitly accepts an alternative validation path.