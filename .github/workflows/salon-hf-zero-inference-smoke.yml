name: Salon HF Zero Inference Smoke (Deprecated)

# Historical official-Qwen Zero test. The official Space currently requests a GPU
# duration above the anonymous Zero-GPU allowance. This known gate is already recorded
# by salon-hf-zero-validation.yml and should not create repeated failing CI runs.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deprecated: official Qwen Space Zero inference has a known duration-policy gate."
          echo "Use salon-hf-zero-validation.yml for current contract checks."
